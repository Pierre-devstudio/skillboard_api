diff --git a/static/menus/skills_collaborateurs.js b/static/menus/skills_collaborateurs.js
index a95881a..51ec9b9 100644
--- a/static/menus/skills_collaborateurs.js
+++ b/static/menus/skills_collaborateurs.js
@@ -486,15 +486,22 @@
 
   function getCollaborateurRoleClass(role) {
     const key = (role || "").toString().trim().toLowerCase();
-    if (key === "manager") return "collab-role-badge collab-role-badge--manager";
-    if (key === "formateur") return "collab-role-badge collab-role-badge--formateur";
-    if (key === "temporaire") return "collab-role-badge collab-role-badge--temp";
-    return "collab-role-badge";
+    if (key === "manager") return "ns-badge ns-badge-role ns-badge-role--manager";
+    if (key === "formateur") return "ns-badge ns-badge-role ns-badge-role--formateur";
+    if (key === "temporaire") return "ns-badge ns-badge-status ns-badge-status--info";
+    return "ns-badge ns-badge-role";
+  }
+
+  function getCollaborateurRoleContextClass(role) {
+    const key = (role || "").toString().trim().toLowerCase();
+    if (key === "manager") return "ns-badge-role--manager";
+    if (key === "formateur") return "ns-badge-role--formateur";
+    return "";
   }
 
   function renderRolePills(it) {
     return getCollaborateurRoles(it)
-      .map(r => `<span class="ns-badge sb-badge ${getCollaborateurRoleClass(r)}">${escapeHtml(r)}</span>`)
+      .map(r => `<span class="${getCollaborateurRoleClass(r)}">${escapeHtml(r)}</span>`)
       .join("");
   }
 
@@ -1437,14 +1444,14 @@
 
               // Le header conserve uniquement les rôles sous forme de badges.
               const roleBadges = [];
-              if (d.is_temp) roleBadges.push({ label: "Temp", cls: "collab-role-badge collab-role-badge--temp" });
-              if (d.ismanager) roleBadges.push({ label: "Manager", cls: "collab-role-badge collab-role-badge--manager" });
-              if (d.isformateur) roleBadges.push({ label: "Formateur", cls: "collab-role-badge collab-role-badge--formateur" });
+              if (d.is_temp) roleBadges.push({ label: "Temp", cls: getCollaborateurRoleClass("Temporaire") });
+              if (d.ismanager) roleBadges.push({ label: "Manager", cls: getCollaborateurRoleClass("Manager") });
+              if (d.isformateur) roleBadges.push({ label: "Formateur", cls: getCollaborateurRoleClass("Formateur") });
 
               const headerBadges = byId("collabModalBadges");
               if (headerBadges) {
                 headerBadges.innerHTML = roleBadges
-                  .map(b => `<span class="ns-badge sb-badge ${escapeHtml(b.cls)}">${escapeHtml(b.label)}</span>`)
+                  .map(b => `<span class="${escapeHtml(b.cls)}">${escapeHtml(b.label)}</span>`)
                   .join("");
               }
 
@@ -1818,7 +1825,7 @@
                     <span>Rôles et responsabilités</span>
                   </div>
                   <div class="sb-collab-roles-grid">
-                    <div class="sb-collab-role-item collab-role-badge--manager">
+                    <div class="sb-collab-role-item ${getCollaborateurRoleContextClass("Manager")}">
                       <span class="sb-collab-summary-icon" aria-hidden="true">${collabIcon("user")}</span>
                       <span>
                         <span class="sb-collab-summary-label">Manager</span>
@@ -1826,7 +1833,7 @@
                       </span>
                     </div>
 
-                    <div class="sb-collab-role-item collab-role-badge--formateur">
+                    <div class="sb-collab-role-item ${getCollaborateurRoleContextClass("Formateur")}">
                       <span class="sb-collab-summary-icon" aria-hidden="true">${collabIcon("graduation")}</span>
                       <span>
                         <span class="sb-collab-summary-label">Formateur</span>
diff --git a/static/novoskill_visual_tokens.css b/static/novoskill_visual_tokens.css
index 6f9a34a..667daf7 100644
--- a/static/novoskill_visual_tokens.css
+++ b/static/novoskill_visual_tokens.css
@@ -11,6 +11,8 @@
   --ns-danger-text: #991b1b; --ns-danger-bg: #fef2f2; --ns-danger-border: #fecaca;
   --ns-info-text: #1e40af; --ns-info-bg: #eff6ff; --ns-info-border: #bfdbfe;
   --ns-neutral-text: #475569; --ns-neutral-bg: #f8fafc; --ns-neutral-border: #cbd5e1;
+  --ns-role-manager-text: var(--ns-info-text); --ns-role-manager-bg: var(--ns-info-bg); --ns-role-manager-border: var(--ns-info-border);
+  --ns-role-formateur-text: #be185d; --ns-role-formateur-bg: #fdf2f8; --ns-role-formateur-border: #fbcfe8;
   --ns-status-active: #22c55e;
   --ns-criticality-low-text: #6d28d9; --ns-criticality-low-bg: #faf5ff; --ns-criticality-low-border: #e9d5ff;
   --ns-criticality-moderate-text: #6d28d9; --ns-criticality-moderate-bg: #f3e8ff; --ns-criticality-moderate-border: #d8b4fe;
@@ -45,6 +47,8 @@
 .ns-badge-criticality--moderate { color:var(--ns-criticality-moderate-text); background:var(--ns-criticality-moderate-bg); border-color:var(--ns-criticality-moderate-border); }
 .ns-badge-criticality--high { color:var(--ns-criticality-high-text); background:var(--ns-criticality-high-bg); border-color:var(--ns-criticality-high-border); }
 .ns-badge-criticality--critical { color:var(--ns-criticality-critical-text); background:var(--ns-criticality-critical-bg); border-color:var(--ns-criticality-critical-border); }
-.ns-badge-role { color:var(--ns-info-text); background:var(--ns-info-bg); border-color:var(--ns-info-border); }
+.ns-badge-role { --ns-role-text:var(--ns-info-text); --ns-role-bg:var(--ns-info-bg); --ns-role-border:var(--ns-info-border); color:var(--ns-role-text); background:var(--ns-role-bg); border-color:var(--ns-role-border); }
+.ns-badge-role--manager { --ns-role-text:var(--ns-role-manager-text); --ns-role-bg:var(--ns-role-manager-bg); --ns-role-border:var(--ns-role-manager-border); }
+.ns-badge-role--formateur { --ns-role-text:var(--ns-role-formateur-text); --ns-role-bg:var(--ns-role-formateur-bg); --ns-role-border:var(--ns-role-formateur-border); }
 .ns-badge-requirement { color:var(--ns-warning-text); background:var(--ns-warning-bg); border-color:var(--ns-warning-border); }
 .ns-badge-category { color:var(--ns-neutral-text); background:var(--ns-neutral-bg); border-color:var(--ns-neutral-border); }
diff --git a/static/skills_portal_theme.css b/static/skills_portal_theme.css
index 4721be4..3d8e034 100644
--- a/static/skills_portal_theme.css
+++ b/static/skills_portal_theme.css
@@ -2095,31 +2095,6 @@ textarea.sb-ctrl{
 }
 
 
-#view-vos-collaborateurs .collab-role-badge{
-  background:#ffffff;
-  border-color:#d1d5db;
-  color:var(--collab-text);
-  font-weight:var(--ns-weight-semibold, 600);
-}
-
-#view-vos-collaborateurs .collab-role-badge--manager{
-  background:rgba(2,132,199,.08);
-  border-color:var(--sb-info);
-  color:var(--sb-info);
-}
-
-#view-vos-collaborateurs .collab-role-badge--formateur{
-  background:rgba(248,47,204,.08);
-  border-color:var(--sb-badge-formateur-border);
-  color:var(--sb-badge-formateur-border);
-}
-
-#view-vos-collaborateurs .collab-role-badge--temp{
-  background:rgba(2,132,199,.06);
-  border-color:#94a3b8;
-  color:#475569;
-}
-
 #view-vos-collaborateurs .collab-preview-actions{
   display:flex;
   flex-direction:column;
@@ -3346,31 +3321,6 @@ textarea.sb-ctrl{
   flex-wrap:wrap;
 }
 
-#modalCollaborateur .sb-collab-modal-badges .collab-role-badge{
-  background:#ffffff;
-  border-color:#d1d5db;
-  color:#111827;
-  font-weight:var(--ns-weight-semibold, 600);
-}
-
-#modalCollaborateur .sb-collab-modal-badges .collab-role-badge--manager{
-  background:rgba(2,132,199,.08);
-  border-color:var(--sb-info);
-  color:var(--sb-info);
-}
-
-#modalCollaborateur .sb-collab-modal-badges .collab-role-badge--formateur{
-  background:rgba(248,47,204,.08);
-  border-color:var(--sb-badge-formateur-border);
-  color:var(--sb-badge-formateur-border);
-}
-
-#modalCollaborateur .sb-collab-modal-badges .collab-role-badge--temp{
-  background:rgba(2,132,199,.06);
-  border-color:#94a3b8;
-  color:#475569;
-}
-
 #modalCollaborateur .modal-x{
   margin-top:4px;
 }
@@ -32946,6 +32896,13 @@ body{
   color:var(--ns-success-text);
 }
 
+#modalCollaborateur .sb-collab-role-item .sb-collab-summary-icon{
+  color:var(--ns-role-text, var(--ns-info-text));
+  background:var(--ns-role-bg, var(--ns-info-bg));
+  border-color:var(--ns-role-border, var(--ns-info-border));
+}
+
+
 #modalCollaborateur .sb-collab-comment-card textarea{
   width:100%;
   min-height:72px;
@@ -33016,12 +32973,3 @@ body{
   background:color-mix(in srgb, var(--sb-candidat) 8%, #ffffff);
 }
 
-#modalCollaborateur .sb-collab-role-item.collab-role-badge--manager .sb-collab-summary-icon{
-  color:var(--sb-info);
-  background:color-mix(in srgb, var(--sb-info) 8%, #ffffff);
-}
-
-#modalCollaborateur .sb-collab-role-item.collab-role-badge--formateur .sb-collab-summary-icon{
-  color:var(--sb-info);
-  background:color-mix(in srgb, var(--sb-info) 8%, #ffffff);
-}
