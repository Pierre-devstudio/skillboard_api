diff --git a/static/menus/skills_analyse.js b/static/menus/skills_analyse.js
index 373dba5..92700d6 100644
--- a/static/menus/skills_analyse.js
+++ b/static/menus/skills_analyse.js
@@ -5628,16 +5628,19 @@ function renderDetail(mode) {
     return Math.max(0, Math.min(100, Number.isFinite(n) ? n : 0));
   }
 
-  function analysePrevisionRingHtml(pct, title, valueText, subText, size = 104) {
+  function analysePrevisionRingHtml(pct, title, valueText, bodyText, opts = {}) {
     const s = analysePrevisionPct(pct);
-    const stroke = size >= 100 ? 10 : 9;
+    const size = Math.max(96, Math.min(140, Number(opts.size || 118) || 118));
+    const stroke = 11;
     const r = (size - stroke) / 2;
     const c = 2 * Math.PI * r;
     const offset = c * (1 - s / 100);
-    const hue = Math.round(120 * (s / 100));
+    const mode = (opts.mode || "good").toString().trim().toLowerCase();
+    const hue = mode === "risk" ? Math.round(120 * (1 - s / 100)) : Math.round(120 * (s / 100));
     const fill = `hsl(${hue} 70% 42%)`;
     return `
-      <div class="card" style="padding:12px; margin:0; display:flex; align-items:center; gap:12px; min-width:240px; flex:1;">
+      <div class="card" style="padding:16px; margin:0; min-width:250px; flex:1; display:flex; flex-direction:column; align-items:center; gap:10px; text-align:center;">
+        <div class="label" style="font-size:12px; font-weight:800; color:#475569;">${escapeHtml(title || "—")}</div>
         <div style="position:relative; width:${size}px; height:${size}px; flex:0 0 ${size}px;">
           <svg width="${size}" height="${size}" viewBox="0 0 ${size} ${size}" aria-hidden="true" style="position:absolute; inset:0;">
             <circle cx="${size / 2}" cy="${size / 2}" r="${r}" fill="none" stroke="#e5e7eb" stroke-width="${stroke}" />
@@ -5646,18 +5649,29 @@ function renderDetail(mode) {
                     transform="rotate(-90 ${size / 2} ${size / 2})" />
           </svg>
           <div style="position:absolute; inset:0; display:flex; align-items:center; justify-content:center;">
-            <div style="font-weight:900; font-size:${size >= 100 ? 26 : 22}px; line-height:1;">${s}<span style="font-size:12px; font-weight:800;">%</span></div>
+            <div style="font-weight:900; font-size:26px; line-height:1; color:#111827;">${s}<span style="font-size:12px; font-weight:800;">%</span></div>
           </div>
         </div>
-        <div style="min-width:0;">
-          <div class="label" style="margin-bottom:4px;">${escapeHtml(title || "—")}</div>
-          <div class="value" style="font-size:22px; line-height:1.1;">${escapeHtml(valueText || "—")}</div>
-          <div class="card-sub" style="margin:4px 0 0 0;">${escapeHtml(subText || "")}</div>
-        </div>
+        <div class="value" style="font-size:20px; line-height:1.1;">${escapeHtml(valueText || "—")}</div>
+        <div class="card-sub" style="margin:0; max-width:330px; line-height:1.35;">${escapeHtml(bodyText || "")}</div>
       </div>
     `;
   }
 
+  function analysePrevisionFirstName(full) {
+    const s = (full || "").toString().trim();
+    if (!s) return "ce collaborateur";
+    return s.split(/\s+/)[0] || "ce collaborateur";
+  }
+
+  function analysePrevisionUniqueSentence(firstName, uniqueCount) {
+    const who = (firstName || "ce collaborateur").toString().trim() || "ce collaborateur";
+    const n = Math.max(0, Math.round(Number(uniqueCount || 0)));
+    if (n <= 0) return `Dans les compétences que ${who} peut transmettre, aucune ne repose uniquement sur ${who}.`;
+    if (n === 1) return `Dans les compétences que ${who} peut transmettre, 1 compétence n’a aucun autre porteur en capacité de la transmettre.`;
+    return `Dans les compétences que ${who} peut transmettre, ${n} compétences n’ont aucun autre porteur en capacité de les transmettre.`;
+  }
+
   function analysePrevisionFragilityBadge(value) {
     const s = analysePrevisionPct(value);
     const hue = Math.round(120 * (1 - s / 100));
@@ -5734,13 +5748,13 @@ function renderDetail(mode) {
     const r = row || {};
     const isPotential = kind === "potential";
     const full = r.full || `${r.prenom_effectif || ""} ${r.nom_effectif || ""}`.trim() || "Collaborateur";
+    const firstName = analysePrevisionFirstName(full);
     const code = (r.codif_client || r.codif_poste || "").toString().trim();
     const poste = (r.intitule_poste || "Poste non renseigné").toString();
-    const service = (r.nom_service || "—").toString();
     const reason = (r.raison_sortie || r.event_kind_label || (isPotential ? "Retraite estimée" : "Sortie prévue")).toString();
     const dateLabel = isPotential
-      ? `Horizon départ : ${analysePrevisionYear(r.exit_date || r.retraite_annee || r.horizon_year)}`
-      : `Départ : ${analysePrevisionDate(r.exit_date)}`;
+      ? `Horizon de départ : ${analysePrevisionYear(r.exit_date || r.retraite_annee || r.horizon_year)}`
+      : `Date de départ : ${analysePrevisionDate(r.exit_date)}`;
 
     const cap = r.transmission_capacity || {};
     const total = Number(cap.total_competences_poste || 0) || 0;
@@ -5751,6 +5765,9 @@ function renderDetail(mode) {
     const impacts = r.other_poste_impacts || {};
     const impactItems = Array.isArray(impacts.items) ? impacts.items : [];
 
+    const transmissionText = `Le niveau de maîtrise requis pour la transmission est présent sur ${transmissibles}/${total} compétence${total > 1 ? "s" : ""}.`;
+    const uniqueText = analysePrevisionUniqueSentence(firstName, uniques);
+
     const impactHtml = impactItems.length ? `
       <div class="table-wrap" style="margin-top:10px;">
         <table class="sb-table">
@@ -5782,7 +5799,7 @@ function renderDetail(mode) {
     ` : `<div class="card-sub" style="margin:8px 0 0 0;">Aucun autre poste ne voit sa fragilité augmenter avec cette personne en moins sur le périmètre filtré.</div>`;
 
     const uniqueList = Array.isArray(cap.unique_competences) && cap.unique_competences.length ? `
-      <div class="card-sub" style="margin:8px 0 0 0;">
+      <div class="card-sub" style="margin:10px 0 0 0;">
         Compétences concernées : ${cap.unique_competences.map(c => `${c.code ? escapeHtml(c.code) + " · " : ""}${escapeHtml(c.intitule || "")}`).join(" ; ")}
       </div>
     ` : "";
@@ -5795,49 +5812,43 @@ function renderDetail(mode) {
     ];
 
     return `
-      <div class="sb-prev-modal-grid">
-        <div class="sb-prev-actions-card">
-          <div class="sb-prev-modal-title">Identification du sortant</div>
-          <div style="display:flex; align-items:center; justify-content:space-between; gap:12px; flex-wrap:wrap; margin-top:8px;">
-            <div style="min-width:0;">
-              <div style="font-weight:800; font-size:18px; line-height:1.2;">${escapeHtml(full)}</div>
-              <div style="display:flex; align-items:center; gap:8px; flex-wrap:wrap; margin-top:8px;">
-                ${code ? `<span class="sb-badge sb-badge-ref-poste-code">${escapeHtml(code)}</span>` : ""}
-                <span class="sb-badge">${escapeHtml(poste)}</span>
-                <span class="sb-badge sb-badge-accent">${escapeHtml(dateLabel)}</span>
-              </div>
+      <div class="sb-prev-modal-grid" style="display:flex; flex-direction:column; gap:12px;">
+        <div class="sb-prev-actions-card card" style="padding:14px; margin:0;">
+          <div class="sb-prev-modal-title" style="margin-bottom:10px;">Identification du sortant</div>
+          <div style="display:flex; flex-direction:column; gap:7px; min-width:0;">
+            <div style="font-weight:700; font-size:17px; line-height:1.2; color:#111827;">${escapeHtml(full)}</div>
+            <div style="display:flex; align-items:center; gap:8px; flex-wrap:wrap; min-width:0;">
+              ${code ? `<span class="sb-badge sb-badge-ref-poste-code">${escapeHtml(code)}</span>` : ""}
+              <span style="font-size:13px; font-weight:700; color:#111827; min-width:0;">${escapeHtml(poste)}</span>
             </div>
-            <div style="display:flex; align-items:center; gap:8px; flex-wrap:wrap; justify-content:flex-end;">
-              <span class="sb-badge">${escapeHtml(service)}</span>
-              <span class="sb-badge">${escapeHtml(reason)}</span>
+            <div class="card-sub" style="margin:0; display:flex; gap:6px; flex-wrap:wrap; align-items:center;">
+              <span>${escapeHtml(dateLabel)}</span>
+              ${reason ? `<span>·</span><span>${escapeHtml(reason)}</span>` : ""}
             </div>
           </div>
         </div>
 
-        <div class="sb-prev-actions-card">
-          <div class="sb-prev-modal-title">Capacité de transmission à son poste</div>
-          <div class="card-sub" style="margin:4px 0 10px 0;">Lecture sur toutes les compétences du poste, sans seuil de criticité.</div>
-          <div style="display:flex; gap:12px; flex-wrap:wrap;">
-            ${analysePrevisionRingHtml(capPct, "Compétences transmissibles", `${transmissibles} / ${total}`, "Expert ou Avancé haut")}
-            ${analysePrevisionRingHtml(uniquePct, "Porteuse unique", `${uniques} / ${transmissibles}`, "Part des compétences transmissibles sans autre porteur")}
+        <div class="sb-prev-actions-card card" style="padding:14px; margin:0;">
+          <div class="sb-prev-modal-title" style="margin-bottom:12px;">Capacité de transmission à son poste</div>
+          <div style="display:grid; grid-template-columns:repeat(auto-fit, minmax(260px, 1fr)); gap:12px;">
+            ${analysePrevisionRingHtml(capPct, "Compétences transmissibles", `${transmissibles} / ${total}`, transmissionText, { mode: "good" })}
+            ${analysePrevisionRingHtml(uniquePct, "Porteur unique", `${uniques} / ${transmissibles}`, uniqueText, { mode: "risk" })}
           </div>
           ${uniqueList}
         </div>
 
-        <div class="sb-prev-actions-card">
-          <div class="sb-prev-modal-title">Impact sur les autres postes</div>
-          <div class="card-sub" style="margin:4px 0 0 0;">Calcul issu du moteur de fragilité des Risques actuels, rejoué avec cette personne en moins.</div>
+        <div class="sb-prev-actions-card card" style="padding:14px; margin:0;">
+          <div class="sb-prev-modal-title" style="margin-bottom:10px;">Impact sur les autres postes</div>
           ${impactHtml}
         </div>
 
-        <div class="sb-prev-actions-card">
-          <div class="sb-prev-modal-title">Sécurisation</div>
-          <div class="card-sub" style="margin:4px 0 10px 0;">Cochez les hypothèses à envoyer dans Simulation RH.</div>
+        <div class="sb-prev-actions-card card" style="padding:14px; margin:0;">
+          <div class="sb-prev-modal-title" style="margin-bottom:12px;">Hypothèses de sécurisation</div>
           <div style="display:grid; grid-template-columns:repeat(auto-fit, minmax(220px, 1fr)); gap:8px;">
             ${scenarios.map(([value, label, checked]) => `
-              <label class="card" style="padding:10px; margin:0; display:flex; gap:8px; align-items:flex-start; cursor:pointer;">
+              <label class="card" style="padding:10px; margin:0; display:flex; gap:8px; align-items:flex-start; cursor:pointer; border-color:#dbe3ef;">
                 <input type="checkbox" data-prev-scenario="${escapeHtml(value)}" data-prev-scenario-label="${escapeHtml(label)}" ${checked ? "checked" : ""} style="margin-top:2px;">
-                <span style="font-weight:700; font-size:13px; line-height:1.25;">${escapeHtml(label)}</span>
+                <span style="font-weight:700; font-size:13px; line-height:1.25; color:#111827;">${escapeHtml(label)}</span>
               </label>
             `).join("")}
           </div>
diff --git a/unified_api/app/services/skills_analyse_engine.py b/unified_api/app/services/skills_analyse_engine.py
index 3357584..467a87f 100644
--- a/unified_api/app/services/skills_analyse_engine.py
+++ b/unified_api/app/services/skills_analyse_engine.py
@@ -2858,7 +2858,13 @@ def _fetch_prevision_transition_capacity(
             a.resultat_eval,
             CASE
                 WHEN lower(trim(COALESCE(ec.niveau_actuel, ''))) IN ('d', 'expert') THEN TRUE
-                WHEN a.resultat_eval IS NOT NULL AND a.resultat_eval >= 63 THEN TRUE
+                WHEN a.resultat_eval IS NOT NULL
+                 AND (
+                    CASE
+                        WHEN a.resultat_eval <= 24 THEN (a.resultat_eval / 24.0) * 100.0
+                        ELSE a.resultat_eval
+                    END
+                 ) >= 63 THEN TRUE
                 ELSE FALSE
             END AS is_transmissible
         FROM poste_comp pc
